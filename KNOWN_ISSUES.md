# 已知问题与限制

> ChatLens 0.2.x 早期内测版。最后更新：2026-07-07

## 平台

- **Windows**：主要开发与测试平台，内测首选。
- **macOS / Linux**：可通过源码构建，尚未提供官方安装包与系统级测试。

## 导入

- **ChatGPT Project 名称**：若导出包中无 `projects.json`（Manifest V1 等旧格式），Project 会显示为「未命名 · …后缀」。
- **会话级时间戳**：部分 ChatGPT 导出会把 `update_time` 批量刷成导出日；ChatLens 会尝试从消息时间推导，但极个别异常时间戳可能仍需手动核对。
- **重复导入**：同一来源重复导入会更新已有对话并去重，不会无限复制；但若导出包本身结构变化，行为请以导入报告为准。
- **实验性数据源**（Codex、Copilot、Grok、DeepSeek 等）：格式可能随平台更新变化，导入失败时请通过 Issues 反馈（勿粘贴聊天正文）。

## 数据与升级

- **数据库迁移**：开发阶段 schema 可能变更；大版本升级前请备份 `chatlens.db` 与 `imports/` 目录。
- **删除数据**：卸载应用不会自动删除库文件；需在应用数据目录手动删除 `chatlens.db` 与 `imports/`（见应用内「隐私与数据」）。
- **单 Library**：当前版本不支持多库切换。

## 性能

- 超大对话（数千条消息）打开时可能较慢，仍在优化中。
- 图库与 Timeline 采用分页加载；首次滚动到底部才会加载更多。

## UI

- 窗口宽度 < 1180px 时右侧对话信息面板隐藏，需通过标题栏「对话信息」链接打开 Drawer。
- 部分 Markdown 复杂格式（嵌套列表、特殊插件语法）渲染可能不完整。

## 反馈

遇到问题请前往 [GitHub Issues](https://github.com/manxisuo/ChatLens/issues)。请描述环境、版本与复现步骤，**不要粘贴完整对话内容**。
